# Screenshots

Drop your own captures here, named:

- `library.png`    — the game library / grid view
- `compiling.png`  — a compile in progress with the live log
- `benchmark.png`  — the benchmark panel showing before/after 1% low + frametime graph

These are referenced by the main README.md.
